import React from "react";
import * as styled from "./CardSkeletonStyles";

const CardSkeleton = ({ size }) => {
  return <styled.Container size={size}></styled.Container>;
};

export default CardSkeleton;
